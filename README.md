# todo-backend

Node.js (Express) backend starter.

## Requirements

- Node.js 18+ (recommended 20+)

## Setup

```bash
npm install
copy .env.example .env
```

## Run

Dev (auto-reload):

```bash
npm run dev
```

Prod:

```bash
npm start
```

## Endpoints

- `GET /health` -> `{ ok: true }`
